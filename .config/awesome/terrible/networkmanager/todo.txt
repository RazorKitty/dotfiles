flatten the object, depending on the view
think about whats actually useful to know

primary connection widget
    we dont actually care about the primary connection, just objects it contains
    devices
        driver
        driver-version
        interface
        device-type
        state
    ip4config + ip6config
        addresses
        family

        

